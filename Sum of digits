#include <bits/stdc++.h>
  using namespace std;
  
  int main()
  {
    int n,r,sum=0;
    cout<<enter number;
    cin>>n;
    while(n>0)
    {r=n%10;
    n=n/10;
    sum=sum+r;
    }
    cout<<sum;
    //write your code here
    
    return 0;
  }
