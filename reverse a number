#include <bits/stdc++.h>
  using namespace std;
  
  int main()
  {
    int i, rev=0, n;
    
    cin>> i;
    while(i>0)
    {
      rev=(rev*10)+i%10;
      i=i/10;
      
    }
    cout<<rev;
    //write your code here
    
    return 0;
  }
